# 阶段 11（11A–11D）短测记录

本记录对应提交 `4b8f4f9`，只覆盖可选 DLSS Ray Reconstruction（RR）最小后端；未实施 11E 或 Frame Generation，也没有运行 600 秒/1800 秒长测。原始命令参数、stdout、stderr、退出码和单行 benchmark JSON 保存在被 `.gitignore` 忽略的仓库相对目录：

`output/stage11/20260812-first-round/`

## 环境与锁定资料

- GPU：NVIDIA GeForce RTX 4060 Laptop GPU
- 驱动：`nvidia-smi` `610.62`；WMI `32.0.16.1062`
- Streamline：v2.12.0
- RR support 原始结果：RR smoke/Debug 为 `rr=1(0)`；SVGF + DLSS 对照由于 RR 未启用，为 `rr=0(31)`。三次运行的完整 stderr 保存在各自 `stderr.txt`。
- `environment.json` 和 `hashes.json` 保存了本机环境和 SHA-256 原始记录。

锁定资料与产物 hash：

| 文件 | SHA-256 |
| --- | --- |
| `external/streamline-v2.12.0/include/sl_dlss_d.h` | `A726E8135F2E36AD3939CE852530A879E1D909B85539C20C8CB5029AB122F2C4` |
| production `sl.dlss_d.dll` | `35E4F8FC8396A08514A4493B87BA507445F886DAD4B7C6C024D3305824236BE6` |
| production `nvngx_dlssd.dll` | `F4E97624F70FBB769ACB11EBD751B512ECC9463D4BD6AEF04896D3956E6084A0` |
| development `sl.dlss_d.dll` | `33050565F417C10C8BB543C5192D21DE0D334FBCFFBB8A62AF0565576F3C7B34` |
| development `nvngx_dlssd.dll` | `FD83687C98D00754AE8E26B5DAA7DABFED04557113C69C9EDDFE4B6F3ACAF426` |
| `target/release/ray_tracing_demo.exe` | `C38688DB1CA355209D18F04DC74CA3F4AE0B5F35585E2FE5CCDD46FE6B52EC79` |
| `target/debug/ray_tracing_demo.exe` | `0C7C9200DBDC41F4A5F0EA2CBDAE02147CB04855FA923A2DF882EAB8654A4BAD` |

## 自动化命令

以下命令均退出码 0：

```text
cargo fmt --all -- --check
cargo test --locked
cargo test --features streamline --locked
cargo test --features streamline-rr --locked
cargo test --all-features --locked
cargo clippy --all-targets --all-features -- -D warnings
cargo build --release --all-features --locked
cargo build --all-features --locked
git diff --check
```

## 有界 GPU smoke

所有 benchmark 均为 `--benchmark-seconds 1`、`1280x720`、baseline AS、optimized command recording、DLSS Quality；没有使用长测。每个 case 的 `exit-code.txt` 为 `0`。

```text
target/release/ray_tracing_demo.exe --benchmark-seconds 1 --output-size 1280x720 --denoiser dlss-rr --upscaler dlss-quality --atrous-mode baseline --command-recording-mode optimized --acceleration-structure-mode baseline
target/release/ray_tracing_demo.exe --benchmark-seconds 1 --output-size 1280x720 --denoiser svgf --upscaler dlss-quality --atrous-mode baseline --command-recording-mode optimized --acceleration-structure-mode baseline
target/debug/ray_tracing_demo.exe --benchmark-seconds 1 --output-size 1280x720 --denoiser dlss-rr --upscaler dlss-quality --atrous-mode baseline --command-recording-mode optimized --acceleration-structure-mode baseline
```

| case | valid samples | Total p50/p95 ms | RR input p50 | RR evaluate p50 | SVGF/NRD/DLSS-SR inactive evidence |
| --- | ---: | ---: | ---: | ---: | --- |
| Release RR Quality | 150 | 3.86 / 4.31 | 0.01 | 2.64 | Temporal/À-Trous/NRD/DLSS compose/evaluate 均 0 samples/null |
| Release SVGF + DLSS Quality | 238 | 3.00 / 3.29 | null | null | RR input/evaluate 均 0 samples/null；Temporal、À-Trous、DLSS SR 有样本 |
| Debug RR + GPU Validation | 186 | 4.04 / 4.23 | 0.02 | 2.78 | Temporal/À-Trous/NRD/DLSS compose/evaluate 均 0 samples/null |

RR smoke JSON 中的输出/内部尺寸为 `1280x720` / `853x480`，显存 usage/budget 为 `414679040 / 7537164288` bytes（Debug 为 `416886784 / 7537164288`）。Debug 运行输出 `D3D12 Debug InfoQueue：0 条消息`。

## 待人工确认

- PENDING MANUAL：真实画面中的 RR 细节、鬼影、镜面稳定性、normal/roughness adapter 视觉质量尚未由人工截图观察确认。
- PENDING MANUAL：未进行 F1/F2 交互、resize/minimize/hot-reload 的人工观察；本轮只完成有界启动与 benchmark smoke。
- Frame Generation、Reflex 2 Frame Warp 及 11E 未实现，留待后续阶段。
