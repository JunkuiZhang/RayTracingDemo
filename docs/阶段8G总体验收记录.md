# 阶段 8G 总体验收记录

## 1. 结论和证据边界

本记录对应 `7f5768f` 之后的 8G 实现，代码提交为：

- `0b960e9 refactor(stage8-validation): type debug views and interaction diagnostics`
- `b3ee2b2 feat(stage8-validation): add fence-safe captures and memory measurement`
- `c8c2475 feat(stage8-validation): add acceptance runner`
- `40acb1b fix(stage8): enforce trustworthy acceptance`（Codex review 修复）
- `ba76cb4 docs(stage8): correct 8G acceptance status`
- `3b87a52 docs(stage8): plan post-review short revalidation`
- `bf0aba3 docs(stage8): record post-review short acceptance`
- `3d55830 docs(stage8): plan validation fixes and paired attribution`
- `7a2a440 fix(stage8-validation): compute medians and environment reliably`
- `69b3c30 test(stage8): record reproducible debug validation evidence`
- `30073cd test(stage8): add paired 1080p attribution benchmark`
- `6a9619e fix(stage8-validation): align paired evidence run root`
- `aa2b0b7 fix(stage8-validation): preserve paired run count parameter`
- `d6a868a docs(stage8): record corrected validation and paired attribution`
- `3ab79f4 docs(stage8): plan authenticated dynamic attribution`
- `0fdcb7f fix(stage8-validation): make paired evidence self-authenticating`
- `067a3f5 test(stage8): add bounded dynamic mode attribution`

目标设备为 NVIDIA GeForce RTX 4060 Laptop GPU。H1–I3 已完成 median、环境来源、Debug raw、runner timeout、自认证 commit paired 和同 EXE dynamic/fixed attribution；没有修改 renderer、Rust 渲染逻辑、HLSL、动态分辨率算法或任何验收阈值。最新自认证数据中 candidate/reference 仅差 `+0.892857%`，dynamic/fixed 仅差 `+0.509554%`，因此没有超过 3% 的 commit regression 或 dynamic-mode regression；但两组 candidate/dynamic median 都高于 `7.6632 ms`，绝对历史门槛仍 FAIL。阶段 8 尚未完成，1800/600 秒、真实 F1/F2/resize/最小化/恢复/hot-reload、公开 PBR/大模型和 PIX UI 仍未执行。最新细则见 [`阶段8G配对归因Review修复与动态模式归因执行方案.md`](阶段8G配对归因Review修复与动态模式归因执行方案.md)。

最终验收目录为：

`output/stage8g/20260809-172437-113b460b/`

仓库相对的汇总文件为 `output/stage8g/20260809-172437-113b460b/summary.json`。该文件由最终 raw benchmark 单行 JSON 后处理生成；原始 runner 外层 PowerShell 在 1800 秒进程结束后的汇总阶段超出工具命令时限，因此 `runner_wrapper_completed=false`。原始 stdout/stderr 仍全部保留在同一目录，但该后处理 summary 只能作为历史索引，不能作为修复后 runner 的最终 PASS。

review 修复后的 3 秒短回归目录为 `output/stage8g/20260809-183211-9b0810ff/`。本轮没有重跑 1800 秒测试；只运行了 3 秒 forced-dynamic smoke 和 320×180、8 SPP Release capture 回归。

本次修复后正式短矩阵目录为 `output/stage8g/20260809-184649-8143bd8e/`，汇总为 `output/stage8g/20260809-184649-8143bd8e/summary.json`。唯一正式 runner 命令为：

```powershell
.\scripts\stage8_acceptance.ps1 `
  -Configuration Release `
  -Runs 3 `
  -Seconds 30 `
  -SkipLongRun
```

runner 从 `18:46:49` 运行至 `18:59:28`（工具墙钟约 760 秒），进程退出码 `1`，`summary.passed=false`；stored summary 因 median bug 写为 `7.69 ms`，raw 重算为 `7.67 ms`，两者都高于 `7.6632 ms`，所以历史失败状态仍成立且不是脚本崩溃。未传 `-LargeModel` 或 `-PbrModel`，未执行任何长测。

## 1.1 Review 修复与 1080p 归因结果

H1 corrected runner 的 `-SelfTest` 输出为 `{"self_test":"passed","cases":5}`，覆盖空数组、单元素、奇数、偶数和七元素集合。修复后的环境字段均记录来源：GPU/Windows driver `32.0.16.1062` 来自 `Win32_VideoController`，电源方案来自 `powercfg /getactivescheme`，AC 来自 `GetSystemPowerStatus`。I1 后 acceptance、Debug 和 paired 子进程均有 case 对应的墙钟上限；paired 还保存 before/after 环境并要求 GPU/driver/电源方案一致且两端均为 AC。

H2 Debug summary：`output/stage8g/debug-20260809-193534-bc7a77b5/summary.json`，run-id 为 `debug-20260809-193534-bc7a77b5`，退出码 0，五条命令均保存 args/stdout/stderr/exit/elapsed/JSON。四条 benchmark 墙钟约为 `95.2/94.4/96.7/97.9 s`，capture 约 `100.6 s`；均为 exit 0、单行 JSON、InfoQueue 0。capture 为 320×180、8 SPP、`object-material-id`、4794 bytes，SHA-256 为 `D01600922474EBCD3612606D79E237275066DEE1181C8AF32095F04073E8F603`。

H3 corrected `Recheck1080` 命令为：

```powershell
.\scripts\stage8_acceptance.ps1 `
  -Configuration Release `
  -Suite Recheck1080 `
  -Runs 3 `
  -Seconds 30 `
  -SkipLongRun `
  -SkipCaptures
```

run-id 为 `20260809-195413-09b167f5`，墙钟约 226 秒，退出码 1，`summary.passed=false`；只执行 smoke、fixed 1080p 三次和 default dynamic 三次，`long_run=null`、`captures=[]`，没有重跑 54/23。raw Total 如下：

| case | raw p50/p95（ms） | 正确 median p95 | 状态 |
|---|---|---:|---|
| forced smoke | 4.64/5.29 | 5.29 | PASS |
| fixed 1920×1080 | 6.41/7.25；6.18/7.06；6.17/7.07 | 7.07 | PASS |
| default dynamic | 7.02/7.76；7.40/7.93；7.49/7.98 | 7.93 | FAIL（绝对门槛） |

smoke 的 measurement 为 `6315 valid + 6 stale = 6321 Total`，3 次 measurement switch、generation/history 为 `3/3`，最终 retired 0、high-watermark 1、idle wait 0；所有 case 显存 query 有效、failed=0、peak ratio 约 7%–8%。

旧 schema v1 paired summary：`output/stage8g/paired-1080-20260809-194700/summary.json`，candidate 为 H3 修复 HEAD `aa2b0b7`，reference archive 的 ZIP comment 经 Codex 事后人工验证为完整 `2031abf`。交错顺序严格为 `candidate-1, reference-1, reference-2, candidate-2, candidate-3, reference-3`；脚本退出码 0，墙钟约 193 秒。该 summary 没有保存 commit/tree/archive/EXE hash，以下 raw 可保留，但它不再作为最终 provenance 证据。

| 侧 | 原始 Total p95（ms） | 正确 median p95 |
|---|---|---:|
| candidate | 7.30；7.47；7.42 | 7.42 |
| reference (`2031abf`) | 6.94；7.34；7.67 | 7.34 |

`candidate_vs_reference = (7.42 / 7.34 - 1) × 100% = 1.089918%`。旧 candidate median `7.42 <= 7.6632`，当时决策为 **HISTORICAL GATE PASS**；同时没有超过 3% paired regression。第一次使用 Windows tar 解包 archive 因中文路径失败，错误目录和错误记录保留；随后使用 ZIP Unicode 解包成功。I1 用下面的 schema v2 run 取代其证据链地位。

## 1.2 自认证 commit 与 dynamic/fixed 归因

I1 自认证 summary 为 `output/stage8g/paired-auth-1080-20260809-202926-33a32298/summary.json`，schema version 2。runner 从 candidate/reference commit 分别执行 `git archive --format=zip`，验证 ZIP comment 后展开到隔离 source/target，使用 `cargo build --release --locked --offline` 同法构建；candidate/reference 构建墙钟分别约 `169.16/155.45 s`。两侧 manifest 如下：

| 字段 | candidate | reference |
|---|---|---|
| commit | `0fdcb7f880c2fa1a811002504c28bca1bc7369df` | `2031abfc1992661b7186b68e58b5ff5f1c3712de` |
| tree | `47fc241d305e42f4b0a46e3ea90dcbb5afdeaa80` | `5055a5b8a29d75f20d5e80c572165ae44108c4c1` |
| archive SHA-256 | `EC081A9C69A015492D500D30E6BC1FB5B18AFDC574FED31F30F88F988E736E51` | `52D3039D988EA7331B4C72E72D387CA3FAB76EC92719459D14E5ADC0BDB0ACD5` |
| EXE SHA-256 | `2368805E9CD10BF7A1680C3CB7AFFB0BF01C0FAB2043B34E33BD189E30F83CF4` | `D3F45454E07D9586681080D08C79B857C4C0AFFB348872974621C2814311C9C9` |

provenance 和环境前后检查均通过；GPU 为 RTX 4060 Laptop，Windows driver 为 `32.0.16.1062`，活动方案为平衡，before/after 均由 `GetSystemPowerStatus` 报告 AC。六条 benchmark 均为 30 秒、无 timeout，raw 为：

| 侧 | 原始 Total p95（ms） | 正确 median p95 |
|---|---|---:|
| candidate `0fdcb7f` | 7.63；7.91；7.96 | 7.91 |
| reference `2031abf` | 7.70；7.84；7.92 | 7.84 |

`candidate_vs_reference = +0.892857%`，低于 3%，因此没有 paired regression；但 candidate `7.91 > 7.6632 ms`，决策为 **ABSOLUTE GATE FAIL / NO PAIRED REGRESSION**。脚本按归因语义 exit 0，整个 archive/build/benchmark 墙钟约 `524.7 s`；其中实际 GPU benchmark 仍只有 `6 × 30 s`。

I2 summary 为 `output/stage8g/paired-resolution-1080-20260809-204146-98dcbc25/summary.json`，读取并重新验证上述 candidate manifest/EXE hash，用同一 EXE 按 `dynamic-1, fixed-1, fixed-2, dynamic-2, dynamic-3, fixed-3` 交错运行：

| 模式 | 原始 Total p95（ms） | 正确 median p95 |
|---|---|---:|
| default dynamic | 7.66；7.89；7.89 | 7.89 |
| fixed 1.0 | 7.81；7.85；7.97 | 7.85 |

三次 dynamic 的 output/render/min/max 均为 1920×1080，measurement switch/downscale/upscale/stale 全为 0，因此可以与 fixed 原生尺寸直接比较。`dynamic_vs_fixed = +0.509554%`，mode regression PASS；dynamic median 仍高于 `7.6632 ms`，所以决策为 **ABSOLUTE GATE FAIL / NO DYNAMIC-MODE REGRESSION**。summary `passed=false`、脚本按双门槛语义 exit 1，墙钟约 `193.1 s`；before/after 环境一致且均为 AC。该结果说明 corrected Recheck 的 `7.93 ms` 不是动态控制器造成的显著 GPU 回退，不构成修改 renderer 的依据。

## 2. 实现摘要

- `DebugView` 使用固定 11 项枚举，CLI、F1 顺序、ToneMap root constant、capture metadata 共用同一映射；resize、F2 和动态控制阻断使用稳定 stderr 诊断行。
- capture 只从 `display_output` 记录 `CopyTextureRegion`，位置在 Total timestamp/PIX event 结束之后、swap-chain copy 之前；readback 由 pending fence 持有，只有 fence 完成后 Map，按 D3D12 footprint row pitch 去除 padding，再原子写 PNG。
- `image_diff` 是独立 binary，Cargo `default-run` 仍为 `ray_tracing_demo`；不兼容尺寸返回非零，成功 stdout 是一行 JSON。
- `VideoMemoryTelemetry` 继续查询实际选中的 adapter 的 `IDXGIAdapter3` local segment，约 500 ms 查询一次；benchmark measurement 固定 60 个 60 秒 checkpoint，统计 start/end/peak usage、minimum budget、peak ratio、valid/failed query。
- capture 进入 pending 后不再提交更晚的帧，确保 capture fence 是退出前最后一个 submitted fence，避免写完 PNG 后销毁仍被后续在飞帧引用的 renderer 资源。
- runner 现在按每个 case 校验 output/render extent、模式、p50/p95、样本数、动态 epoch、generation 创建/切换/回收、history reset、idle wait、显存峰值和性能门槛；image diff 必须满足像素阈值，而不再只检查 exit code。
- 正式矩阵改为 1920×1080 output：固定 scale 校验量化后的 render extent，forced dynamic 必须在 measurement 内发生切换；AS 截图使用仓库 glTF fixture，11 个 DebugView 均执行 command/AS baseline-vs-optimized diff。
- 8F 未实现；没有修改 Path Trace、随机采样、Temporal、À-Trous、ToneMap 或动态分辨率控制公式。

## 3. 自动检查

最终代码提交均执行了以下检查并通过：

```text
cargo fmt --all -- --check                 PASS
cargo test --all-targets                   PASS (101 个主 binary 测试 + 4 个 image_diff 测试)
cargo clippy --all-targets --all-features -- -D warnings  PASS
cargo build                                PASS
cargo build --release                      PASS
git diff --check                           PASS
PowerShell AST parse                       PASS
3 秒 1920×1080 forced-dynamic smoke        PASS
320×180、8 SPP Release capture             PASS
```

短 smoke 在 measurement 内发生 1 次降档，Total p95 `5.77 ms`，generation create/switch/retire 为 `1/1/1`，最终 retired 为 0、high-watermark 为 1、idle wait 为 0，显存峰值占预算 `7.35%`。该结果只验证修复后的 runner 和动态生命周期断言可运行，不代替三次 30 秒正式矩阵或 30 分钟稳定性测试。

## 4. Release RTX 4060 Laptop 数据

以下保留 Luna 原始 30 秒数据，表中是三次 raw JSON 的中位数。数字格式为 `p50/p95`，单位 ms；AS 为静态 Cornell，因此为 `0/0`。它们没有全部满足 8G 正式验收口径：default dynamic 实际 output 为 1280×720；1080p fixed p95 `8.00 ms` 相对 8E-2 同硬件三次结果中的最差 `7.44 ms` 回退约 `7.5%`，超过允许的 `3%`（门槛 `7.6632 ms`）。

| 配置 | output → render | 有效样本/次数 | Total | AS | Path Trace | Temporal | À-Trous 聚合 | À-Trous 0/1/2/3 | ToneMap |
|---|---|---:|---:|---:|---:|---:|---:|---|---:|
| 720p fixed 1.0 | 1280×720 → 1280×720 | 7187 / 3 | 3.33/3.59 | 0/0 | 1.57/2.17 | 0.79/1.00 | 0.91/1.40 | 0.24/0.65, 0.22/0.23, 0.22/0.22, 0.23/0.23 | 0.04/0.04 |
| 900p fixed 1.0 | 1600×900 → 1600×900 | 5699 / 3 | 5.14/5.83 | 0/0 | 2.46/3.00 | 0.66/1.19 | 1.86/2.40 | 0.39/0.79, 0.37/0.77, 0.38/0.78, 0.39/0.79 | 0.06/0.08 |
| 1080p fixed 1.0 | 1920×1080 → 1920×1080 | 3968 / 3 | 7.52/8.00 | 0/0 | 3.59/3.92 | 0.97/1.57 | 2.74/3.16 | 0.56/1.06, 0.54/1.05, 0.54/1.02, 0.57/1.08 | 0.10/0.36 |
| default dynamic | 1280×720 → 1280×720 | 7195 / 3 | 3.23/3.58 | 0/0 | 1.50/2.15 | 0.70/0.93 | 0.91/1.61 | 0.24/0.64, 0.22/0.59, 0.22/0.24, 0.23/0.59 | 0.04/0.04 |

固定内部尺寸档位也执行过一次 30 秒 raw benchmark，但 output 是 1280×720；它只能作为量化尺寸诊断，不能替代要求的 1920×1080 output 固定 scale 矩阵：

| output | scale | render extent | Total p50/p95 |
|---|---:|---|---:|
| 1280×720 | 0.83 | 1056×592 | 2.44/2.75 |
| 1280×720 | 0.75 | 960×536 | 2.04/2.36 |
| 1280×720 | 0.67 | 856×480 | 1.46/1.67 |

### 4.1 修复后正式短矩阵 raw Total

下表直接来自修复后 runner 的 `summary.json`，数字为每个 case 的原始有效样本数与 Total `p50/p95`（ms）；`median p95` 只对该 case 已执行的 raw runs 取中位数。没有删除失败 case，也没有重跑 1080p fixed。

| case | output → render | 原始 runs（n，p50/p95 ms） | median p95 | 状态 |
|---|---|---|---:|---|
| forced dynamic smoke | 1920×1080 → 1536×864（最终） | 6413，4.46/5.30 | 5.30 | PASS |
| fixed 1280×720 | 1280×720 → 1280×720 | 7198，3.19/3.48；7198，3.17/3.47；7198，3.19/3.48 | 3.48 | PASS |
| fixed 1600×900 | 1600×900 → 1600×900 | 6051，4.83/5.46；6098，4.79/5.47；6056，4.81/5.51 | 5.47 | PASS |
| fixed 1920×1080 | 1920×1080 → 1920×1080 | 4230，7.05/7.63；4129，7.24/7.69；4176，7.15/7.67 | **7.67** | **FAIL（3% 门槛）** |
| fixed scale 0.83 | 1920×1080 → 1592×896 | 5839，5.00/5.67 | 5.67 | PASS |
| fixed scale 0.75 | 1920×1080 → 1440×808 | 7051，4.11/4.65 | 4.65 | PASS |
| fixed scale 0.67 | 1920×1080 → 1280×720 | 7196，3.18/3.39 | 3.39 | PASS |
| default dynamic | 1920×1080 → 1920×1080 | 4268，6.97/7.48；4255，6.97/7.64；4416，6.63/7.43 | 7.48 | PASS（≤16.67 ms） |
| glTF static baseline | 1280×720 → 1280×720 | 7198，3.20/3.50 | 3.50 | PASS |
| glTF animated baseline | 1280×720 → 1280×720 | 7198，3.24/3.53 | 3.53 | PASS |
| glTF static optimized | 1280×720 → 1280×720 | 7198，3.21/3.51 | 3.51 | PASS |
| glTF animated optimized | 1280×720 → 1280×720 | 7198，3.24/3.53 | 3.53 | PASS |

所有 benchmark stdout 均为恰好一行 JSON；Total 与各 pass 的有效样本数一致，p50/p95 有限且 p95 不低于 p50。固定 case 的 generation create/switch/retire、extent change、history reset 和 idle wait 均为 0。强制动态 smoke 的 measurement 为 `6407 valid + 6 stale = 6413 Total`，发生 3 次 measurement/4 次 lifetime 降档，generation create/switch/retire 为 `3/3/3`，最终 retired 为 0、high-watermark 为 2、idle wait 为 0；default dynamic 三次均保持 1920×1080，measurement/lifetime switch 均为 0。

每个 benchmark 都有有效显存 query（61 次、failed=0、结束 inactive、checkpoint count 与数组长度一致）；peak usage ratio 约 `4.3%–8.4%`，全部低于 70%。小型 `NonIndexedMultiNode.gltf` 的 19 个 BLAS 在 optimized case 中均为 `no_allocation_saving`，committed allocation 没有节省，不能把 reported compacted bytes 当作显存收益。

正式矩阵生成 54 张实际 128 SPP capture，均有有效 PNG、路径、SHA-256 和元数据；22 个 Exact diff 全部 `changed_rgb/alpha/max/RMSE=0`，1 个 À-Trous tolerance diff 的最差 `alpha=0、max abs=1、RMSE=0.0070904784`，全部通过固定阈值。capture/diff 记录没有失败项。

## 5. 强制动态 smoke 和 1800 秒长测

旧实现先运行了强制动态 `--dynamic-resolution --target-gpu-ms 4` 的 30 秒 smoke；raw JSON 通过当时有限的 valid sample、dynamic epoch 和有限数值检查。旧长测命令为：

```text
target\release\ray_tracing_demo.exe --benchmark-seconds 1800 --command-recording-mode optimized --atrous-mode baseline --acceleration-structure-mode baseline --output-size 1280x720 --dynamic-resolution --target-gpu-ms 4
```

旧长测结果：

- 运行约 1800 秒，`valid_samples=431567`，动态 measurement `valid_samples=431567`，`stale_generation_samples_ignored=0`，`switch_count=0`。
- output/render 始终为 1280×720，generation create/switch/retire 均为 0（初始 generation 不计入 measurement），`gpu_idle_wait_count=0`，history reset 为 0。
- Total `3.16/3.48`，AS `0/0`，Path Trace `1.42/1.85`，Temporal `0.41/0.86`，À-Trous 聚合 `0.91/1.51`，四轮分别 `0.24/0.64`、`0.22/0.59`、`0.22/0.24`、`0.23/0.59`，ToneMap `0.04/0.06` ms。
- 显存 measurement 有 31 个 checkpoint（0、60、…、1800 秒），valid query 3584、failed query 0；start usage 326,074,368 B，end/peak usage 327,131,136 B，minimum budget 7,537,164,288 B，peak ratio 4.3402%。

由于 Total p95 `3.48 ms` 低于 forced target 的 high threshold `5 ms`，measurement 内 `switch_count=0`。因此这份数据可以证明 720p 固定工作负载下 30 分钟显存没有持续增长、idle wait 为 0，但没有覆盖动态 generation 生命周期，不能称为 forced-dynamic 长测通过。修复后的 runner 使用 1920×1080 output，并要求 measurement 内至少一次真实动态切换。本轮按用户要求未重跑该长测。

## 6. Deterministic capture 和 image diff

旧 Release capture 矩阵共生成 53 张 PNG，全部位于 `output/stage8g/20260809-172437-113b460b/captures-matrix/`，每张为 1280×720、实际 128 SPP、RTX 4060 Laptop GPU。覆盖：

- command-recording baseline/optimized × 11 DebugView；
- acceleration-structure baseline/optimized × 11 DebugView；
- À-Trous baseline/shared final；
- fixed scale 0.83/0.75/0.67 final；
- dynamic default/forced 的 final 和 history-length。

后处理执行了 23 个 image diff（11 个 command 对、11 个 AS 对、1 个 À-Trous 对），现有 JSON 全部为零。原始 diff JSON 在 `output/stage8g/20260809-172437-113b460b/diffs-matrix/`。但是旧 runner 源码只对最后一个 command view 执行 diff，且 `image_diff` 即使像素不同也会 exit 0；旧 runner 因而不能自动重现或强制这 23 个阈值。修复后 exact 对要求 changed/alpha/max/RMSE 全零，À-Trous 对要求 alpha 为零、max abs 不超过 1、RMSE 不超过 0.10。

此外，旧 AS capture 命令没有传 `--model`，实际仍是 Cornell，并未覆盖 glTF AS baseline/optimized。修复后的矩阵显式加载 `assets/gltf/Triangle/NonIndexedMultiNode.gltf`，但本轮未执行完整 128 SPP 矩阵，因此 glTF AS capture/diff 仍待重跑。

这证明了 capture 源、PNG row-pitch 解包和这些 CLI 参数组合之间的确定性；它不证明用户实际按键 F1/F2，也不替代人工画面观察。

## 7. Debug GPU Validation

H2 证据脚本已执行 Debug + GPU-Based Validation 的方案五条命令，summary 和每条 raw args/stdout/stderr 均保存在 `output/stage8g/debug-20260809-193534-bc7a77b5/`；五条命令均为 exit code 0，且 stderr 末尾均为 `D3D12 Debug InfoQueue：0 条消息`：

1. baseline command recording + baseline À-Trous + baseline AS，运行约 95.2 秒；
2. optimized command recording + baseline À-Trous + forced dynamic，运行约 94.4 秒，`gpu_idle_wait_count=0`；
3. `NonIndexedMultiNode.gltf` static + optimized AS，运行约 96.7 秒；
4. 同一 glTF animated + optimized AS，运行约 97.9 秒，`gpu_idle_wait_count=0`；
5. glTF optimized AS、`object-material-id`、8 SPP readback，运行约 100.6 秒；PNG 为 320×180、4794 bytes，SHA-256 为 `D01600922474EBCD3612606D79E237275066DEE1181C8AF32095F04073E8F603`。

上述 CLI/capture 仍不能冒充真实 F1 键盘输入或可信 GUI 画面观察。未执行完整的 Debug Validation 交互矩阵（11 个真实 F1 输入、F2、用户 resize、最小化/恢复、shader hot reload），因此该人工/交互门槛为 PENDING MANUAL/BLOCKED，而不是 PASS。

## 8. PASS / FAIL / BLOCKED

| 验收项 | 状态 | 说明 |
|---|---|---|
| Rust 自动测试、Clippy、Debug/Release build、diff check | PASS | 101 + 4 tests，`-D warnings` 通过 |
| 修复后 3 秒 1080p 强制动态 smoke | PASS（短回归） | measurement 内 1 次切换，Total p95 5.77 ms，代际/历史/显存对账通过 |
| Release 720/900 三次矩阵 | PASS | 修复后 raw；正确 Total p95 median 分别为 3.48/5.47 ms |
| Release 1080p fixed 三次矩阵 | FAIL | 三次 raw 为 7.63/7.69/7.67 ms，正确 median 7.67 ms，超过 7.6632 ms |
| 默认 dynamic 三次 30 秒 | PASS（数值） | 1920×1080 output，正确 Total p95 median 7.48 ms；三次未触发切换 |
| 修复后 forced dynamic smoke | PASS | 1920×1080 output，measurement 内真实切换，代际/历史/显存对账通过 |
| 1800 秒/600 秒及其他长测 | NOT EXECUTED | 本轮硬性禁止；旧 1800 秒仅为 720p、switch 0 的历史诊断 |
| fence-safe PNG capture 机制 | PASS | 修复退出 fence 边界；Release 54 张和 Debug 8 SPP readback 成功 |
| 完整 128 SPP capture/diff 矩阵 | PASS | 54 captures、23 diffs；22 exact 全零，À-Trous tolerance 满足阈值 |
| Debug GPU-Based Validation 五条短命令 | PASS（raw 可复核） | H2 summary 保存五条 args/stdout/stderr/exit/JSON，均 exit 0、InfoQueue 0 |
| corrected `Recheck1080` 汇总 | FAIL（真实门槛） | fixed median 7.07；default dynamic median 7.93 > 7.6632 ms；未执行 capture/diff |
| 旧 schema v1 paired attribution | HISTORICAL RAW ONLY | candidate 7.42、reference 7.34、差 +1.089918%；archive 后验正确，但 summary 不自认证 |
| schema v2 commit paired attribution | ABSOLUTE GATE FAIL / NO PAIRED REGRESSION | candidate 7.91、reference 7.84、差 +0.892857%；provenance/environment PASS |
| 同 EXE dynamic/fixed attribution | ABSOLUTE GATE FAIL / NO DYNAMIC-MODE REGRESSION | dynamic 7.89、fixed 7.85、差 +0.509554%；dynamic 全程原生、switch 0 |
| 真实 F1 11 项输入和画面人工观察 | BLOCKED | CLI `--debug-view` capture 不能冒充 F1 |
| F2、resize、最小化/恢复、hot reload Debug 交互矩阵 | BLOCKED | 本轮没有可信人工观察/完整 Debug 序列 |
| BoxTextured/DamagedHelmet 或其他公开 PBR 大模型 | BLOCKED | 本地没有资产，未联网下载 |
| PIX UI capture / PIX 画面对比 | BLOCKED | 未安装或自动操作 PIX UI |
| 阶段 8 总体完成 | NOT COMPLETE | 1080p 绝对历史门槛、长时动态、真实 F1/F2/resize/最小化/恢复/hot-reload、公开 PBR/大模型和 PIX UI 仍未关闭；8F 按入口规则未实施 |

## 9. 建议 review 位置

优先 review：

1. `src/renderer/d3d12.rs` 的 `record_capture_copy`、`poll_pending_capture` 和 `render` 中 Total 结束到 swap-chain copy 的边界，重点检查 readback resource、union 中临时 COM clone 和 resize/close 生命周期。
2. `src/renderer/d3d12/memory.rs` 的 `VideoMemoryMeasurement` checkpoint/失败保留逻辑，重点检查 benchmark epoch 开始时的强制查询和 60 槽上限。
3. `src/realtime/windows_app.rs` 的 capture 退出、CloseRequested shutdown 和 DPI/物理像素尺寸路径。
4. `scripts/stage8_acceptance.ps1` 的 case expectations、image diff 阈值和 1080p/glTF 矩阵；短回归已通过，正式运行仍需在稳定 AC/空闲 GPU 环境下完成。
